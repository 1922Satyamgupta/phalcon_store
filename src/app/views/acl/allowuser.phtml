<h2>add order using this form</h2>

<?php echo $this->tag->form("acl/buildacl"); ?>

   
    <p>
        <label for="role">role</label>
        <?php echo $this->tag->select(["role", Roles::find(), 'using' => ['role','role'] ]); ?>
    </p>
    <?php 
    $data = Addcontrols::find() ;
    foreach($data as $k =>$v) {
        echo '
        <input type="checkbox" id="vehicle1" name="component[]" value="'.$v->id.'">
        <label for="vehicle1">'.$v->controller.' '.$v->action.'</label><br>' ;
        
    }
     ?>


    <p>
        <?php echo $this->tag->submitButton("allow user"); ?>
    </p>

</form>